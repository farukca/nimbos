$("#<%= @todolist.todop_type %>_<%= @todolist.todop_id %>_todolists").prepend('<%= j render(@todolist) %>');
$('#new_todolist').each(function() { this.reset(); });
jQuery(document).ready(function() {       
  App.initAjax();
  FormComponents.init();
});