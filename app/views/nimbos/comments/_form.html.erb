<div class="row-fluid">
  <%= form_for [object, object.comments.new], :url => nimbos.comments_path("#{object.class.name}_id" => object.id), html: {id: "new_comment_#{object.class.name}_#{object.id}"}, remote: true do |f| %>
  <%#= simple_form_for([object, object.comments.new], :url => nimbos.comments_path("#{object.class.name}_id" => object.id), validate: true, html: { class: "form-horizontal" }) do |f| %>
    <div class="row-fluid">
    <%#= f.error_notification %>
    <%#= f.input :comment_text, :as => :text, :input_html => {:rows => "1", :style => "width:95%"}, :label => "Yorum Ekle" %>
      <%#= label_tag 'comment_text', t("defaults.link.new_comment"), class: "control-label" %>
      <div class="row-fluid">
        <%= f.text_field :comment_text, placeholder: t("defaults.link.new_comment"), hint: "Press enter to post", class: "input-block-level" %>
        <%#= f.submit(t("defaults.action.share"), class: 'btn btn-info') %>
      </div>

    </div>
  <% end %>
</div>
